//Test4: Expressions

class Test3 {
    void main(){
        if (1<2)
        return true;
        ENDIF
        
        if (2>1)
        return false;
        ENDIF

        while (2<=2){
            while (2>=2){
                a++;
            }
        }

        if (2==2)
            if (a != b)
                if (a+b*c-!(d*e))
                    a = (a ? b -: c +: d);
                ENDIF
            ENDIF
        ENDIF 
    }
}